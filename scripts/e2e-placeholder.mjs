// Placeholder for the end-to-end suite.
// The real Playwright suite covering the critical paths (see CLAUDE.md) lands
// with issue #10. Until then this script exits 0 so the CI e2e job stays green.
console.log("[e2e] No end-to-end suite yet — placeholder until issue #10. Skipping.");
process.exit(0);
